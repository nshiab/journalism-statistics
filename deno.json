{
  "name": "@nshiab/journalism-statistics",
  "version": "1.5.2",
  "description": "Useful TypeScript functions for journalism projects. This library focuses on statistical calculations.",
  "repository": "https://github.com/nshiab/journalism-statistics",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "tasks": {
    "all-tests": "deno install && deno fmt --check && deno lint && deno check src/index.ts && deno doc --lint src/index.ts && deno publish --allow-dirty --dry-run && TZ=America/Toronto deno test -A --fail-fast && deno task llm",
    "llm": "deno doc --json src/index.ts > docs.json && deno run -A src/generateLlmMd.ts && deno fmt llm.md",
    "test-coverage": "deno test -A --fail-fast --coverage=cov_profile && deno coverage cov_profile",
    "patch": "deno task all-tests && deno run -A jsr:@nshiab/bump-version patch",
    "minor": "deno task all-tests && deno run -A jsr:@nshiab/bump-version minor",
    "major": "deno task all-tests && deno run -A jsr:@nshiab/bump-version major"
  },
  "exclude": [
    "npm/"
  ],
  "publish": {
    "exclude": [
      "test",
      ".github",
      "npm"
    ]
  },
  "nodeModulesDir": "auto",
  "imports": {
    "@nshiab/journalism-format": "jsr:@nshiab/journalism-format@^1.1.3",
    "jstat": "npm:jstat@^1.9.6"
  },
  "compilerOptions": {
    "lib": [
      "dom",
      "deno.ns"
    ]
  },
  "lint": {
    "include": [
      "src/**/*.ts"
    ]
  }
}
